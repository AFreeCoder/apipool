# 上游同步评审记录（2026-05-26）

## 基线

- 当前分支：`main`
- 当前 HEAD：`f382382b8a974bc239bed1e3e02ea14a956e7dca`
- 合入前本地基线：`e7654d693af2da96db4d866f957974e1e1c586eb`
- 上游引用：`upstream/main`
- 上游 SHA：`9ef144874adc7217dda3e7e43f42e344b01e30dd`
- merge-base：`2f70d965bf5b046ad6e9474a77a493bf4fb60801`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.131`
- upstream/main 版本文件：`0.1.131`
- 本地最终 VERSION：`0.1.131`

## 上游更新摘要

- 吸收 upstream/main 自 `2f70d965` 之后的 4 个非 merge 提交：
  - `6b39b344`：新增用户 × 平台 USD 配额，包含 Ent schema、migration、后台设置、用户配额管理、计费累加与前端展示。
  - `33ac8eb2`：OpenAI HTTP 上游启用 HTTP/2 策略，增加响应头超时、代理不兼容时 H2 -> H1 fallback，以及 OpenAI request profile。
  - `23f3d426`：内容审计风险阈值配置，扩展 RiskControlView 与后端内容审计设置。
  - `9ef14487`：同步 `backend/cmd/server/VERSION` 到 `0.1.131`。
- 高风险模块命中：`backend/internal/service/openai_gateway_service.go`、`backend/internal/service/gateway_service.go`、`backend/internal/repository/http_upstream.go`、`frontend/src/views/admin/SettingsView.vue`、`deploy/config.example.yaml`、`deploy/docker-compose*.yml`、`backend/cmd/server/VERSION`。
- 版本链路一致：upstream 最新 tag 为 `v0.1.131`，upstream/main 版本文件为 `0.1.131`，本地最终 `VERSION` 为 `0.1.131`。

## 本地定制保护点

- 保留 APIPool 的 Kiro/OpenClaw 扩展：`GatewayService` 构造器同时保留 `kiroTokenProvider`，并接入上游新增的 `userPlatformQuotaRepo`。
- 保留 APIPool 的购买入口与顶部跑马灯：`SettingsView.vue` 的 `purchase_subscription_*`、`marquee_*` 字段与上游 `default_platform_quotas` 并存。
- 保留现有部署/回滚主线：本轮只吸收上游对环境变量和 config 示例的新增项，没有改动 `.github/workflows/deploy.yml`、`deploy/rollback.sh`、`deploy/docker-compose.deploy.yml`。
- 保留 OpenAI/Codex 兼容链路，同时吸收上游 OpenAI HTTP/2 profile、响应头超时和平台配额计费累加。

## 冲突与取舍

- Git 冲突文件：
  - `backend/cmd/server/wire_gen.go`：合并 Kiro provider 参数和上游 `serviceUserPlatformQuotaRepository` 参数。
  - `backend/internal/service/gateway_service.go`：结构体初始化保留 Kiro 字段，并新增 `userPlatformQuotaRepo`。
  - `backend/internal/service/gateway_record_usage_test.go`：测试构造器补齐新增 quota repo 参数。
  - `frontend/src/views/admin/SettingsView.vue`：表单类型同时保留 APIPool 购买/跑马灯字段与上游平台限额字段。
  - `frontend/src/views/admin/__tests__/SettingsView.spec.ts`：测试 i18n stub 同时保留跑马灯与平台限额翻译键。
- 额外修复：
  - `backend/internal/handler/gateway_models_test.go` 补齐 `NewGatewayService` 新参数，修复 unit 构建失败。
  - `frontend/src/api/admin/settings.ts` 的 `defaultAuthSourceDefaultsValue()` 补齐 `platform_quotas`，修复 `vue-tsc` 类型错误。
- 无冲突语义复核：`openai_gateway_service.go` 的 OpenAI request profile 标记、`gateway_service.go` 的余额计费平台配额累加、`deploy/*.yml` 的新增 OpenAI HTTP2 环境变量。

## 测试记录

- 通过：`cd backend && go generate ./ent`
- 通过：`cd backend && go test -tags=unit ./...`
- 通过：`cd backend && make test-unit`
- 通过：`cd backend && go test -tags=integration ./...`
- 通过：`cd backend && make test-integration`
- 通过：`cd backend && golangci-lint run ./...`（0 issues）
- 通过：`pnpm --dir frontend run lint:check`
- 通过：`pnpm --dir frontend run typecheck`
- 通过：`pnpm --dir frontend run test:run`（112 files / 683 tests）
- 通过：`git tag --merged upstream/main --sort=-version:refname | head -1` -> `v0.1.131`
- 通过：`cat backend/cmd/server/VERSION` -> `0.1.131`
- 通过：`cd backend && make build`（`main.Version=0.1.131`）
- 通过：`POSTGRES_PASSWORD=dummy JWT_SECRET=dummy-jwt-secret-which-is-long-enough REDIS_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`
- 通过：`POSTGRES_PASSWORD=dummy JWT_SECRET=dummy-jwt-secret-which-is-long-enough REDIS_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`
- 通过：`POSTGRES_PASSWORD=dummy JWT_SECRET=dummy-jwt-secret-which-is-long-enough REDIS_PASSWORD=dummy docker compose -f deploy/docker-compose.yml config -q`
- 通过：`bash deploy/version_resolver.sh resolve .` -> `0.1.131`
- 通过：`bash scripts/collect_upstream_sync_context.sh --no-fetch`
- 部署后版本输出 / 页面版本人工核对：待 `apipool-push-deploy` 推送部署后执行。

## 剩余风险与观察点

- 新增用户 × 平台 USD 配额会影响余额计费用户的额度判定与消费累加；订阅计费路径按上游逻辑豁免，需要上线后观察 `user_platform_quota` 相关错误日志和用户侧配额展示。
- OpenAI HTTP/2 默认启用，提供环境变量快速回退：`GATEWAY_OPENAI_HTTP2_ENABLED=false` 或保留自动 fallback 到 HTTP/1.1。
- 内容审计风险阈值配置只影响后台风险控制配置项，需部署后确认前端保存/读取正常。
- 回滚路径仍沿用现有 `deploy/rollback.sh image` 和部署前 DB/image 备份机制。

## 结论

- 建议保留当前 merge 结果并进入代码评审/部署流程。当前本地回归、构建、版本链路和 Compose 配置校验均已通过。
