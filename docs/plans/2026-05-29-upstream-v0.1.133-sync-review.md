# 上游同步评审记录（2026-05-29）

## 基线

- 当前分支：`codex/upstream-sync-20260529`
- 当前 HEAD：`0bda447bdcd1b0c6a7d9e9348293999f6619571c`（评审前文档提交）
- 合并提交：`e59244471e3bb3e43552cf4d273bd3246990dcd3`
- 合入前本地基线：`0c03e9c75acd81cb4b26f4945947e213372d18ac`
- 上游引用：`upstream/main`
- 上游 SHA：`f18451e56f15b31ef602ab238037b56c3522b19f`
- merge-base：`8c1a07852cb32b147d005458fd0d38e612011a2f`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.133`
- upstream/main 版本文件：`0.1.133`
- 本地最终 VERSION：`0.1.133`

## 上游更新摘要

- 本轮从 `0.1.132` 同步到 `0.1.133`，吸收 upstream-only commits 36 个。
- 主要更新：
  - user x platform 配额链路新增 DB 写聚合 flusher、sentinel 回填，减少无配额行用户 preflight 时每请求回源 DB。
  - OpenAI 账号支持按 5h / 7d 用量阈值自动暂停调度，并在后台运营高级设置中暴露默认阈值。
  - OpenAI / Codex 兼容新增 `codex_cli_only` 放行 Claude Code Codex 插件的客户端识别。
  - OpenAI WS、Responses/Chat 转换、Anthropic beta `context_management` sanitize、Gemini messages tool/text block 顺序、count_tokens payload 过滤、usage request context 等网关修复。
  - 价格元数据更新，新增 `claude-opus-4-8` 映射和相关测试。
- 高风险模块：`backend/internal/handler/openai_gateway_handler.go`、`backend/internal/service/openai_gateway_service.go`、`frontend/src/views/admin/SettingsView.vue`、运营高级设置、user platform quota、OpenAI account scheduler、版本链路。

## 本地定制保护点

- 品牌与文案：本轮没有接受 upstream 对 APIPool 文档/Logo/帮助页的大规模回退；最终 diff 未删除现有 APIPool 文档入口、购买入口、OpenClaw/Kiro 相关前端资产。
- 部署/回滚/版本链路：`backend/cmd/server/VERSION` 对齐 `0.1.133`；`.github/workflows/deploy.yml`、`deploy/rollback.sh`、`deploy/version_resolver.sh`、`deploy/docker-compose.deploy.yml` 未被 upstream 删除或回退，后续部署前仍需按 `apipool-push-deploy` 复核。
- OpenAI OAuth / Codex 兼容：保留 APIPool 既有 OpenAI gateway、Responses、WS、Codex compact fallback、`codex_cli_only` 兼容链路，同时吸收 upstream 的 allowed client 和 capability gate 修复。
- 后台入口与默认配置：`SettingsView.vue` 保留 APIPool 的购买订阅、跑马灯和本地设置语义；运营高级设置同时保留 `ignored_error_codes` 白名单并吸收 OpenAI 配额自动暂停默认阈值字段。
- Kiro / OpenClaw：本轮自动合并后仍保留 Kiro account create/edit、OAuth、OpenClaw 配置导入与相关测试文件；需要在 review 阶段重点确认 upstream 对账号表单的改动没有遗漏 Kiro 分支。

## 冲突与取舍

- Git 冲突文件：
  - `backend/internal/handler/gateway_handler.go`：合并并发错误处理，使用 upstream 新增 `concurrencyErrorResponse` 入口。
  - `backend/internal/handler/openai_gateway_handler.go`：同上，统一 OpenAI gateway 的并发错误收口。
  - `backend/internal/service/ops_settings_models.go`：同时保留 APIPool `ignored_error_codes` 与 upstream `openai_account_quota_auto_pause`。
  - `frontend/src/views/admin/ops/components/OpsSettingsDialog.vue`：同时补默认 `ignored_error_codes` 和 quota auto pause 默认对象，避免旧 payload 下表单绑定失败。
- 语义取舍：
  - 并发错误处理吸收 upstream 对 `context.Canceled` 返回 499 的新语义，但保留 APIPool 对 Redis / cache / network acquire 异常返回 503 且文案为 `Concurrency system unavailable (cache or network issue), please retry later` 的线上排障语义。
  - `auth_service_email_bind_test.go` 的测试 stub 补齐 `BatchSnapshotUsage`，对齐 user platform quota repository 新接口。
- 无冲突但复核的热点：`openai_gateway_service.go`、`openai_gateway_handler_test.go`、`SettingsView.vue`、`CreateAccountModal.vue`、`EditAccountModal.vue`、`wire.go` / `wire_gen.go`。

## 测试记录

- 通过：`cd backend && go test -tags=unit ./...`
- 通过：`cd backend && make test-unit`
- 未通过（环境 / 外部依赖）：`cd backend && go test -tags=integration ./...`
  - `internal/middleware`、`internal/server/routes` 的 Redis testcontainers 用例失败，因为本机 Docker daemon 未运行：`Cannot connect to the Docker daemon at unix:///Users/afreecoder/.docker/run/docker.sock` / `unix:///var/run/docker.sock`。
  - `internal/pkg/tlsfingerprint` 的外部探针用例失败，因为 `https://tls.peet.ws/api/all` TLS handshake EOF。
  - 这次失败不表现为本轮代码断言失败；属于本机 Docker / 外部网络探针条件不足。
- 未跑：`cd backend && make test-integration`
  - 原因：直接等价入口 `go test -tags=integration ./...` 已因 Docker daemon 未运行和外部 TLS 探针 EOF 失败，重复 wrapper 不会增加新信号。
- 通过：`cd backend && golangci-lint run ./...` -> `0 issues.`
- 通过：`pnpm --dir frontend install --frozen-lockfile`
  - 首轮前端检查失败原因是隔离 worktree 缺 `frontend/node_modules`；安装后重跑通过。
- 通过：`pnpm --dir frontend run lint:check`
- 通过：`pnpm --dir frontend run typecheck`
- 通过：`pnpm --dir frontend run test:run`
  - `116` 个测试文件、`708` 个测试通过。
- 通过：`cd backend && go test -tags=unit ./internal/handler ./internal/service`
  - 首轮失败：`emailBindQuotaRepoStub` 缺 `BatchSnapshotUsage`；`concurrency_error_response_test.go` 仍期待 upstream 泛化 503 文案。
  - 修复后通过：`internal/handler` 23.787s；`internal/service` 90.973s。
- 通过：`git tag --merged upstream/main --sort=-version:refname | head -1` -> `v0.1.133`
- 通过：`cat backend/cmd/server/VERSION` -> `0.1.133`
- 通过：`make build`
  - 后端构建版本：`0.1.133`
  - 前端生产构建通过；Vite 仅输出既有 chunk size / dynamic import 警告。
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`
- 通过：`bash deploy/version_resolver.sh resolve .` -> `0.1.133`
- 通过：`bash scripts/collect_upstream_sync_context.sh --no-fetch`
  - `latest_upstream_tag: v0.1.133`
  - `local_version: 0.1.133`
  - `upstream_version: 0.1.133`
  - `upstream_only_commits: 36`
- 待补：部署后版本输出 / 页面版本人工核对

## 剩余风险与观察点

- 本地 integration 未完成，原因是 Docker daemon 未运行和外部 TLS 指纹探针 EOF；部署前无法用本机 integration 作为强证据，只能依赖 unit/lint/build/compose + 生产部署健康检查。
- OpenAI WS、Responses、account scheduler、quota flusher 变更面较大，review 重点是：用量写聚合是否丢账、自动暂停是否误停账号、Codex allowed client 是否过宽、Kiro 账号分支是否被账号表单重排影响。
- 双评审反馈处理：
  - 已修复：`/v1/messages` fallback 到 group default model 时改用 `SelectAccountWithSchedulerForCapability(... OpenAIEndpointCapabilityChatCompletions ...)`，避免选中 embeddings-only OpenAI 账号。
  - 已补测：`TestOpenAIGatewayService_SelectAccountWithScheduler_Enabled_ChatCompletionsSkipsEmbeddingsOnlyAccount`。
  - 已修复：quota flusher 在 `BatchSnapshotUsage` 普通失败后回填 dirty keys 时改用新的短超时 context，避免 DB 写入 deadline 取消后连带导致 Redis dirty readd 失败。
  - 已补测：`TestFlusher_UpsertDeadlineExceededReaddsWithFreshContext`。
  - 已记录但未扩大修复：FK violation 混合批次仍按 upstream 取舍丢弃整批 dirty key；当前 `database.user_platform_quota_flusher_enabled=false`，不影响默认生产行为，启用 flusher 前建议单独做逐行隔离或有效用户过滤。
- 部署前按 `apipool-push-deploy` 复核 GHCR / DigitalOcean 流程：DB backup、rollback image、remote HEAD、容器健康、线上 `/health` 与公开版本。

## 结论

- 已按双评审处理发布阻断项和文档 HEAD 漂移；若复测通过，可在明确 integration 环境限制后继续部署，并把生产健康检查作为最终强验证。
