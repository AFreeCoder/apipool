# 上游同步评审记录（2026-05-28）

## 基线

- 当前分支：`codex/upstream-sync-20260528`
- 代码同步提交：`675d9fd862b75981a3c8972b005f1e01be195073`
- 合入前本地基线：`42a9f1584f42aa076e181575f599f2106d94e8a9`
- 上游引用：`upstream/main`
- 上游 SHA：`8c1a07852cb32b147d005458fd0d38e612011a2f`
- merge-base：`9ef144874adc7217dda3e7e43f42e344b01e30dd`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.132`
- upstream/main 版本文件：`0.1.132`
- 本地最终 VERSION：`0.1.132`

## 上游更新摘要

- 吸收 upstream/main `9ef14487..8c1a0785`，最新 release tag 为 `v0.1.132`，本地 `backend/cmd/server/VERSION` 已同步到 `0.1.132`。
- 主要功能更新：分组自定义 `/v1/models` 模型列表、OpenAI embeddings gateway、前置拦截审核运行态、后台账号创建时间列、账号池同账号重试状态码配置。
- 主要修复更新：OpenAI WS rate limit 账号 failover、API key responses SSE body fallback、OAuth 401 不覆盖凭据 JSONB、billing long-context cache multiplier、模型 404 按账号模型组合冷却。
- Ops/风控更新：本地策略拒绝与分组拒绝归入 business-limited，count-tokens 从 metrics errors 排除，risk control 文案与测试补齐。
- 高风险模块：OpenAI gateway/WS、rate limit service、billing、content moderation、group/admin schema 与前端后台组配置页面。

## 本地定制保护点

- 版本链路：`backend/cmd/server/VERSION` 与 upstream `v0.1.132` 对齐，`deploy/version_resolver.sh resolve .` 输出 `0.1.132`。
- OpenAI/Codex 配置：保留 APIPool 默认 provider、`OPENAI_DEFAULT_MODEL_ID` 的 `gpt-5.5` 默认值、Responses WebSocket V2 配置，并吸收 upstream 新增 `features.goals = true`。
- OpenAI gateway 本地语义：保留 APIPool usage/billing 的 upstream error event 防护，避免无图片用量或上游错误事件误入账。
- WebSocket 生命周期：保留 APIPool `closeOpenAIClientWS` 只发送 close frame 的收口方式，同时吸收 upstream 的失败账号集合与 failover exhausted 行为。
- Ops 语义：保留本地 `OpenAIStreamTerminalEventForwardedKey`，并吸收 upstream business-limited reason 扩展。

## 冲突与取舍

- `backend/internal/handler/openai_gateway_handler.go`：合并 upstream WSV2 failover loop、`releaseAccountSlot`/`ensureUserSlotHeld`，保留 APIPool WS close 与 usage 防护语义。
- `backend/internal/handler/ops_error_logger.go`：采用 upstream 对 nested error code 的 `strings.TrimSpace` 解析，同时保留本地 ops error 分类路径。
- `backend/internal/handler/ops_error_logger_test.go`：同时保留本地嵌套 code、ignored error codes、legacy message 派生码测试，并加入 upstream nested string code 测试。
- `backend/internal/service/ops_upstream_context.go`：合并 upstream 新增 business-limited reasons，保留本地 OpenAI stream terminal marker。
- `frontend/src/components/keys/UseKeyModal.vue`：保留 APIPool/OpenAI/Codex 默认配置，吸收 upstream goals feature。
- `frontend/src/components/keys/__tests__/UseKeyModal.spec.ts`：扩展断言覆盖 `gpt-5.5`、WSV2、goals feature 与非 WS APIPool provider。
- 额外适配：`backend/internal/service/openai_ws_forwarder_ingress_session_test.go` 的 fatal error event 测试调整为确认 rate limit 返回 `UpstreamFailoverError`，匹配 upstream 新的 WS failover 语义。

## 测试记录

- 通过：`cd backend && go generate ./ent`
- 通过：`cd backend && go test -tags=unit ./...`
- 通过：`cd backend && make test-unit`
- 通过：`cd backend && go test -tags=integration ./...`
- 通过：`cd backend && make test-integration`
- 说明：第一次 integration 全量运行中 `TestAuthRegisterRateLimitThresholdHitReturns429` 因 Docker/testcontainers Redis 启动检查超时失败；随后最小复跑该测试通过，完整 integration 重跑通过。
- 通过：`cd backend && golangci-lint run ./...`
- 通过：`pnpm --dir frontend install --frozen-lockfile`
- 通过：`pnpm --dir frontend run lint:check`
- 通过：`pnpm --dir frontend run typecheck`
- 通过：`pnpm --dir frontend run test:run -- src/components/keys/__tests__/UseKeyModal.spec.ts`，当前 Vitest 实际执行全量 116 个测试文件、701 个测试用例，全部通过。
- 通过：`git tag --merged upstream/main --sort=-version:refname | head -1` 输出 `v0.1.132`
- 通过：`cat backend/cmd/server/VERSION` 输出 `0.1.132`
- 通过：`make build`，Vite 输出既有 dynamic import/chunk size warning。
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`
- 通过：`bash deploy/version_resolver.sh resolve .` 输出 `0.1.132`
- 通过：`bash scripts/collect_upstream_sync_context.sh --no-fetch`
- 待部署后核对：GitHub Actions、服务器容器版本、公网 `/health` 与后台版本显示。

## 剩余风险与观察点

- 上游新增 `group.models_list_config` schema 与 migration，部署时重点观察数据库 migration、后台分组编辑页保存/读取，以及 `/v1/models` 分组定制输出。
- OpenAI embeddings、responses SSE fallback、WS rate limit failover 均触达 gateway 核心路径，部署后需观察 gateway error logs、账号切换日志、billing usage 是否异常。
- OAuth 401 不覆盖凭据 JSONB、重新授权不清空 Extra 配置触达账号凭据生命周期，部署后抽查账号管理重新授权流程。
- billing long-context cache multiplier 改动触达计费，部署后可抽样对比 cache_creation/cache_read 用量倍率。
- Docker compose 配置解析需要真实 `POSTGRES_PASSWORD`，本地验证使用占位变量，仅验证模板结构。

## 结论

- 建议保留当前同步结果并进入代码评审与部署流程。本轮冲突已按 APIPool 本地定制优先级合并，关键后端、前端、构建、lint、compose 与版本链路验证均已通过。
