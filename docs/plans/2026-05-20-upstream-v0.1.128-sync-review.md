# 上游同步评审记录（2026-05-20，v0.1.128）

## 基线

- 当前分支：`codex/upstream-sync-20260520`
- 合入提交：`80e49cb6ecf1c7fc156c8ba3791c743bef14edaa`
- 合入前本地基线：`cc8b54a88d20dfbef9722cc96730a32535157956`
- 上游引用：`upstream/main`
- 上游 SHA：`f5a2ad688af7451a90eec0d9a221ef4442ed54f6`
- merge-base：`7ec61eb2f5d4d7c182ca3e465dfe0c4f806a0ad8`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.128`
- upstream/main 版本文件：`0.1.128`
- 本地最终 VERSION：`0.1.128`

## 上游更新摘要

- 本轮 upstream-only 只有 1 个提交：`f5a2ad68 chore: sync VERSION to 0.1.128 [skip ci]`。
- 实际 upstream diff 仅修改 `backend/cmd/server/VERSION`，从 `0.1.127` 对齐到 `0.1.128`。
- `upstream/main` 最新 release tag、上游 `VERSION` 与本地最终 `VERSION` 均为 `v0.1.128` / `0.1.128`，不存在 tag 与 VERSION 漂移。
- 命中的高风险模块只有版本链路：`backend/cmd/server/VERSION`。

## 本地定制保护点

- 品牌与文案：本轮未触及 `README*`、前端文案、首页、文档入口或 logo，APIPool 品牌不受影响。
- 部署/回滚：本轮未触及 `.github/workflows/deploy.yml`、`deploy/rollback.sh`、`deploy/docker-compose.deploy.yml`、`deploy/version_resolver.sh`；部署备份与 rollback 镜像链路不变。
- 购买入口与跑马灯：本轮未触及 `purchase_subscription_*`、`/purchase` iframe 入口、`marquee_*` 设置或前端布局。
- OpenAI / Codex / Kiro / OpenClaw：本轮未触及网关、OAuth、token refresh、Kiro/OpenClaw 或账号调度代码。
- 版本链路：本地最终 `backend/cmd/server/VERSION` 和 `bash deploy/version_resolver.sh resolve .` 均为 `0.1.128`，上线后左上角/运行时版本预期升级到 `0.1.128`。

## 冲突与取舍

- `git merge upstream/main` 无冲突，merge commit 只带入 `backend/cmd/server/VERSION`。
- 没有语义重叠文件；`scripts/collect_upstream_sync_context.sh --no-fetch` 显示 `File Overlap` 和 `High-risk Overlap` 均为 `<none>`。
- 没有新增配置、接口、路由、UI 入口、部署脚本或依赖变更。

## 测试记录

- `scripts/collect_upstream_sync_context.sh --no-fetch`：通过；`head=80e49cb6ecf1c7fc156c8ba3791c743bef14edaa`，`local_version=0.1.128`，`upstream_version=0.1.128`，`upstream_only_commits=1`。
- `git diff --check HEAD^1..HEAD`：通过。
- `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.128`。
- `cat backend/cmd/server/VERSION`：`0.1.128`。
- `git show upstream/main:backend/cmd/server/VERSION`：`0.1.128`。
- `bash deploy/version_resolver.sh resolve .`：`0.1.128`。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- `cd backend && go test -tags=unit ./...`：通过。
- `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- `make build`：通过；Vite 输出既有 dynamic import / chunk size warning，无构建失败。
- `make test-unit` / `make test-integration` / `go test -tags=integration ./...`：本轮未单独执行。原因是 upstream 实际只改 `VERSION`，且 v0.1.127 完整同步和部署前已刚跑过 unit、integration、lint、前端 lint/typecheck/test 与 build；本轮补跑了版本链路、后端 unit、lint、compose 和完整 build。
- `pnpm --dir frontend run lint:check` / `typecheck` / `test:run`：本轮未单独执行；前端无文件变更，`make build` 已覆盖 `vue-tsc -b && vite build`。
- 部署后版本输出 / 页面版本人工核对：本轮未部署生产，未执行线上版本核对。

## 剩余风险与观察点

- 业务行为风险很低：本轮只有运行时版本号变化，没有功能、配置、路由、账单、OAuth、账号调度或部署链路变化。
- 如果后续发布生产，预期可见变化是版本从 `0.1.127` 升到 `0.1.128`；仍需走 `apipool-push-deploy` 核对 GitHub Actions、双库备份、rollback metadata、live commit、live version 和容器健康。

## 结论

- 建议保留当前 upstream sync 结果。本轮仅同步 upstream `v0.1.128` 的 `VERSION` bump，版本链路已对齐，APIPool 长期定制没有被触碰。
