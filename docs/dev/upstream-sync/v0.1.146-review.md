# 上游同步评审记录（2026-07-07）

## 基线

- 当前分支：`main`
- 当前 HEAD：`26d617efde222e64f1fd8c4f7cef7b9837519a26`
- 合入前本地基线：`26d617efde222e64f1fd8c4f7cef7b9837519a26`
- 上游引用：`upstream/main`
- 上游 SHA：`6f43986c376d76144cb39c7a562c179e19ac7439`
- merge-base：`67e945f8e83ab65d0ebdc3c5bcf8a8549f74e1fd`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.146`
- upstream/main 版本文件：`0.1.146`
- 本地最终 VERSION：`0.1.146`
- 版本对齐结论：本轮 upstream/main 已包含 `backend/cmd/server/VERSION=0.1.146`，本地最终值与 tag 一致，不需要额外拆分版本提交。

## 上游更新摘要

- 吸收 upstream `v0.1.146` 相关更新，共 103 个 upstream-only commits。
- 大块结构性拆分：`setting_handler.go`、`admin_service.go`、`openai_gateway_service.go`、`openai_ws_forwarder.go`、`gateway_service.go`、`usage_log_repo.go`、语言包等被拆为多文件。
- 新增/完善批量生图工作流：后端 ent schema、migrations、repository/service/handler、前端批量生图入口、模型/分组权限与测试。
- OpenAI/Codex/compact/WS 链路新增多项修复：compact body signal、Responses compact 入站端点、OAuth passthrough、function_call id、WS error event、usage 解析与请求体解析日志。
- 前端新增 i18n 分域拆分、侧边栏滚动持久化、批量导入强化、批量生图入口隐藏逻辑和计划编辑 CNY 预览。
- 依赖与安全更新：AWS SDK 升级、audit exception 延期、赞助资源更新。

## 本地定制保护点

- 品牌与文案：保留 APIPool README/默认站点名/本地前端入口语义，避免回退到上游 Sub2API 文案。
- 部署与版本链路：保留本地 Dockerfile/deploy 约定，`VERSION` 对齐 `0.1.146`，`deploy/version_resolver.sh resolve .` 输出 `0.1.146`。
- OpenAI OAuth / Codex：保留 slash-prefixed UA 归一化、compact fallback model、OAuth passthrough body normalization、null output 归一化、failed event 终止标记、gpt-image-2 token billing 开关。
- Kiro 本地扩展：恢复 Kiro token provider/wire、Kiro quota 判定、Kiro forward 生产入口，避免上游拆分后仅剩测试调用。
- 后台设置：保留 Codex hardening partial update 语义，未提交字段不清空，显式提交空字符串仍可清空；保留 advanced scheduler reset-only 校验口径。
- 请求日志/网关统计：保留 request body parse log、stable endpoint fallback、OpenAI selection unsupported model 错误信息。

## 冲突与取舍

- Git 冲突主要集中在配置、handler 拆分、admin service 拆分、OpenAI gateway/WS 拆分、settings、frontend i18n 与批量生图入口。
- 对移动拆分文件采取“接受上游拆分结构，逐项恢复 APIPool 行为”的方式，避免把大文件回滚成旧形态。
- `openai_gateway_*`：保留上游新增 CC pipeline、compact body signal、batch/image support，同时恢复本地 compact fallback、OAuth input normalize、null output normalize、UA normalize、failed terminal mark、gpt-image-2 token billing。
- `gateway_forward.go`：恢复 Kiro 分支在 Anthropic APIKey passthrough 后、Bedrock 前的生产入口，避免 Kiro 相关实现被 lint 判为 unused。
- `setting_handler_update.go`：采用指针字段和生效值校验，解决 partial update 意外清空 Codex hardening 字段。
- 前端测试适配：`BulkEditAccountModal` 断言同步到当前中文“透传”预设文案；`DashboardView` 测试 mock `useBatchImageAccess`，避免组件挂载时依赖真实 Pinia。

## 测试记录

- 通过：`cd backend && go test -tags=unit ./... -count=1`
- 通过：`cd backend && go test -tags=integration ./... -count=1`
- 通过：`cd backend && go test -tags=integration ./internal/service -count=1`（Kiro 入口恢复后补跑）
- 通过：`cd backend && golangci-lint run ./...`
- 通过：`pnpm --dir frontend run lint:check`
- 通过：`pnpm --dir frontend run typecheck`
- 通过：`pnpm --dir frontend run test:run`（139 files / 897 tests）
- 通过：`git tag --merged upstream/main --sort=-version:refname | head -1` -> `v0.1.146`
- 通过：`cat backend/cmd/server/VERSION` -> `0.1.146`
- 通过：`bash deploy/version_resolver.sh resolve .` -> `0.1.146`
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`
- 通过：`make build`
- 通过：`bash scripts/collect_upstream_sync_context.sh --no-fetch`
- 通过：`git diff --check`
- 未单独执行：`cd backend && make test-unit`、`cd backend && make test-integration`；本轮已直接执行其对应的 `go test -tags=unit ./...` 与 `go test -tags=integration ./...`。
- 未执行：部署后版本输出 / 页面版本人工核对；留到 push deploy 阶段完成。

## 剩余风险与观察点

- 批量生图新增 migrations 较多，部署后需观察数据库迁移、冻结余额、队列恢复、失败重试与下载路径。
- OpenAI/Codex/WS 相关文件经历大规模拆分，虽然单测/integration 已覆盖，部署后仍需重点观察 compact、OAuth passthrough、WS error event 和 gpt-image-2 billing。
- 前端 production build 通过但仍有 Vite chunk size / dynamic import 警告，属于既有打包结构提示，本轮未调整 chunk 策略。
- Compose config 需要 `POSTGRES_PASSWORD`，本地静态校验使用 dummy 值通过；真实部署应使用生产 env。

## 结论

- 建议保留当前合入结果并进入双 review。
- 合入结果已通过后端 unit、后端 integration、golangci-lint、前端 lint/typecheck/test、Compose config、version resolver 和 production build。
