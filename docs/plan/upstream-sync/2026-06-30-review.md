# 上游同步评审记录（2026-06-30）

## 基线

- 当前分支：`main`
- 当前 HEAD：`a7ab3aece224f4929d845f936a37b8c46c568fce`
- 合入前本地基线：`a7ab3aece224f4929d845f936a37b8c46c568fce`
- 上游引用：`upstream/main`
- 上游 SHA：`dc1bc15457db60bceabb90fd5b8e2120b449f906`
- merge-base：`c275422251e72750bebe53e41fcf59db7f83fe6b`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.141`
- upstream/main 版本文件：`0.1.141`
- 本地最终 VERSION：`0.1.141`

## 上游更新摘要

- 从 `upstream/main` 吸收 `c275422251e72750bebe53e41fcf59db7f83fe6b..dc1bc15457db60bceabb90fd5b8e2120b449f906`，对应上游版本提升到 `0.1.141`。
- 上游变更覆盖 OpenAI/Codex/Grok 网关、用量与费用统计、支付退款链路、后台 ops system log、内容审核字段、前端用量/支付/Key 页面与多语言 README。
- 高风险命中模块：OpenAI OAuth/Responses/WebSocket 兼容、Grok OpenAI-compatible 入口、`AccountTestService` 构造器、`NewOpsHandler` 构造器、usage log schema/migration、支付 provider 回调与退款、前端 `UsageView`/`UsageTable`。

## 本地定制保护点

- `README.md` 保持 APIPool 品牌、部署/回滚/开发说明和本地平台能力描述，同时吸收上游 Grok OpenAI-compatible 入口更新。
- `backend/cmd/server/VERSION` 已更新为上游最新 `0.1.141`，避免前端持续提示新版本。
- OpenAI OAuth/Codex 本地定制保留：`codexCLIVersion = "0.125.0"`、Codex direct group、root alias reqlog capture、system/developer instruction 抽取、typed content 归一化。
- 上游 Kiro token provider 与本地 Grok token provider 在 wire 生成结果中同时保留。
- `scripts/scaffold_sync_review_doc.sh` 修正为 merge 未提交状态下优先读取 `MERGE_HEAD`，避免复用旧 merge commit 的第二父提交写错上游 SHA。

## 冲突与取舍

- `README.md`：手工重建 APIPool 文档，保留本地部署/回滚/版本链路，更新 Grok 支持范围为 `/v1/messages`、`/v1/chat/completions`、`/chat/completions`、`/v1/responses`、`/responses`、`/backend-api/codex/responses` 与 WebSocket。
- `backend/cmd/server/wire_gen.go`：重新生成，确认 `NewAccountTestService` 同时传入 Kiro、Grok、Antigravity provider。
- `backend/internal/config/config.go` / `config_test.go`：调度权重同时保留 `reset` 与 `quota_headroom`，补齐负值与全零校验。
- `backend/internal/server/routes/gateway.go`：采用上游 Grok-compatible 路由放行，同时保留本地 root alias reqlog capture 与 Codex direct group 行为。
- `backend/internal/service/openai_gateway_service.go`：同时保留上游 `openaiPlatformAPIInputTokensURL` 与本地 `codexCLIVersion`。
- `frontend/src/views/user/UsageView.vue`：接受上游组件化拆分，tooltip 行为迁移到 `UsageTable.vue`，并保留新增的 `UserErrorRequestsTable`。
- 测试构造器漂移：补齐 `NewAccountTestService` 和 `NewOpsHandler` 新参数；Codex transform 测试断言更新为 typed `input_text` content。

## 测试记录

- `pnpm --dir frontend run lint:check`：通过。
- `pnpm --dir frontend run typecheck`：通过。
- `cd backend && go test -tags=unit ./internal/handler/admin`：通过（修复构造器漂移后）。
- `cd backend && go test -tags=unit ./internal/service`：通过（修复 Codex typed content 测试断言后）。
- `cd backend && go test -tags=unit ./internal/service -run 'Test(PrepareRefundRejectsPendingRefundOrder|ExecuteRefundRejectsPendingRefundOrder|QueryAndFinalizeRefund)'`：通过（修复 code review 指出的 `REFUND_PENDING` 重复退款/重复扣减风险后）。
- `cd backend && go test -tags=unit ./internal/service`：通过（完成 receiving code review 修复后复跑）。
- `cd backend && go test -tags=unit ./...`：通过。
- `cd backend && make test-unit`：通过。
- `cd backend && go test -tags=integration ./...`：通过。
- `cd backend && make test-integration`：通过。
- `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- `pnpm --dir frontend run test:run`：通过，127 files / 773 tests。
- `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.141`。
- `cat backend/cmd/server/VERSION`：`0.1.141`。
- `bash scripts/collect_upstream_sync_context.sh --no-fetch`：通过，确认本地与上游版本均为 `0.1.141`。
- `make build`：通过；前端构建仅有既有 chunk/dynamic import 警告。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。未带占位变量时会按预期因 `POSTGRES_PASSWORD is required` 失败。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。未带占位变量时会按预期因 `POSTGRES_PASSWORD is required` 失败。
- `bash deploy/version_resolver.sh resolve .`：`0.1.141`。
- 待执行：部署后线上版本输出 / 页面版本人工核对。

## 剩余风险与观察点

- OpenAI/Codex/Grok 网关入口变化较集中，部署后重点观察 `/v1/messages`、`/v1/chat/completions`、`/chat/completions`、`/v1/responses`、`/responses` 与 WebSocket 请求的 reqlog、错误率和计费记录。
- usage log、ops system log、content moderation、Grok quota 新增 migration，部署后需要确认迁移执行完成且后台列表可正常查询。
- 支付/退款 provider 变更较多，部署后优先 smoke 支付结果页、订单恢复、退款回调与后台订单表。
- 双 review 指出的 `REFUND_PENDING` 重复退款/重复扣减阻塞问题已修复：pending 不再允许普通 refund 重入，pending 查询成功/失败终态化前先用条件状态更新 claim。
- 前端 `UsageView` 大幅组件化，部署后需要真实浏览器核对用户用量页、错误请求表和 tooltip。
- 若 GitHub Actions 或线上 smoke 失败，按现有 `deploy/rollback.sh` 回滚到上一稳定镜像。

## 结论

- 当前冲突已解决，本地定制已复核，本地 unit/integration/lint/build/compose/version 门禁已通过；双 review 的阻塞问题已按 receiving code review 修复并补回归测试。可进入 push deploy。
