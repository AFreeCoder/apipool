# 上游同步评审记录（2026-07-06）

## 基线

- 当前分支：`main`
- 双 review 输入 HEAD：`6ca6f5bf7bb77ac57d19bd18cb39612db7f37ee9`
- receiving review 修复提交：本提交（`fix(sync): address v0.1.145 review findings`）
- 验证修复提交：`c8afdf732f209201ea8dd77dc78ae24697d7de54`
- 合入前本地基线：`6f125d3a1bac18e7e1cee52a44d54e9ec994009d`
- 上游引用：`upstream/main`
- 上游 SHA：`67e945f8e83ab65d0ebdc3c5bcf8a8549f74e1fd`
- merge-base：`b650bdd68d25bad3e502b2e34efe775555da2eba`
- 同步方式：`git merge upstream/main`，随后追加 `fix(sync): address v0.1.145 validation issues`
- merge commit：`cbe00b699256470c042e806a1e06af75f81c252c`
- upstream 最新 release tag：`v0.1.145`
- upstream/main 版本文件：`0.1.145`
- 本地最终 VERSION：`0.1.145`

## 上游更新摘要

- 吸收 upstream `v0.1.145` 范围内 41 个 upstream-only commits，主要包括：
  - API Key 账号请求头覆写（Anthropic/OpenAI）及审计修复。
  - OpenAI 新模型 `gpt-5.6-sol` / `terra` / `luna` 映射。
  - OpenAI 高级调度器控制项、粘性加权、订阅优先级和调度审计修复。
  - EasyPay 自定义支付方式、内建支付方式精确匹配、订阅 CNY 汇率显式 opt-in。
  - API key 并发统计、Anthropic 自定义模型组配置、Antigravity token 刷新、usage CSV BOM、支付响应 NUL byte 清理。
- 命中的高风险模块：
  - OpenAI 网关与 Codex 兼容：`backend/internal/handler/openai_gateway_handler.go`、`backend/internal/service/openai_codex_transform.go`、`backend/internal/service/openai_gateway_service.go`。
  - 调度和限流：`backend/internal/service/openai_account_scheduler.go`、`backend/internal/service/ratelimit_service.go`。
  - 设置与支付：`backend/internal/handler/admin/setting_handler.go`、`backend/internal/service/setting_service.go`、`frontend/src/views/admin/SettingsView.vue`。
  - 部署样例和版本链路：`deploy/.env.example`、`deploy/config.example.yaml`、`deploy/docker-compose*.yml`、`backend/cmd/server/VERSION`。
  - 品牌/文档入口：`README.md`、`README_CN.md`、`README_JA.md`、`frontend/src/components/layout/AppSidebar.vue`。

## 本地定制保护点

- 品牌与文案：保留 `README.md` 的 APIPool 项目说明、生产部署/回滚说明和 iframe 充值页并存提示；不吸收 upstream 通用 Sub2API systemd/Docker 部署长段落。
- 部署/回滚/版本链路：保留本地 DigitalOcean/GHCR 自动部署约定，`backend/cmd/server/VERSION` 对齐到 `0.1.145`，`bash deploy/version_resolver.sh resolve .` 输出 `0.1.145`。
- OpenAI OAuth / Codex 兼容：保留本地 `requestPlatform`、compact path、Codex/OpenAI 兼容处理，并把 upstream 新增的 `previousResponseCanMove` 参数补入遗漏 fallback 调用。
- Kiro / OpenClaw 本地扩展：前端账号表单 import 合并为 Kiro credentials 与 upstream header override 并存，未移除 Kiro 相关能力。
- 后台入口与默认配置：保留 APIPool 的 `purchase_subscription_*` iframe 充值入口、marquee、自定义菜单、web search emulation、risk/cyber session 字段，同时接入 upstream 新增 scheduler 权重和支付 CNY 汇率字段。

## 冲突与取舍

- Git 冲突文件：
  - `README.md`：保留 APIPool 部署/支付并存说明，不合入 upstream 通用部署段落。
  - `backend/internal/config/config.go`：合并本地 `reset` / `quota_headroom` 权重和 upstream `previous_response` / `session_sticky` 权重，全部纳入非负校验和全零校验。
  - `backend/internal/handler/admin/setting_handler.go`：两处 settings payload 都合成并集，保留本地字段并补入 upstream scheduler/CNY 汇率字段。
  - `backend/internal/service/token_refresh_service_test.go`：保留本地 plan sweep stub，并补入 upstream `UpdateExtra` repo stub 行为。
  - `frontend/src/components/account/CreateAccountModal.vue`、`EditAccountModal.vue`、`__tests__/credentialsBuilder.spec.ts`：Kiro credentials 与 header override import 并存。
  - `frontend/src/views/admin/SettingsView.vue`：表单类型保留 purchase/marquee，并补入 upstream scheduler 细项。
- 合入后验证修复：
  - `backend/internal/handler/openai_gateway_handler.go` fallback 调度调用补 `previousResponseCanMove=false`。
  - `backend/internal/service/token_refresh_service_test.go` 两处 `NewTokenRefreshService` 测试调用补 `SchedulerCache=nil`。
  - `backend/internal/service/openai_account_scheduler_test.go` 调度测试调用补 `previousResponseCanMove=false`。
  - `backend/internal/config/config_test.go` 全零权重测试补清 `PreviousResponse` / `SessionSticky`。
- 无冲突但复核的热点：
  - `deploy/docker-compose*.yml` 和 `deploy/config.example.yaml` 可通过 Compose config 与版本解析。
  - `frontend/src/views/admin/SettingsView.vue` 通过 lint/typecheck/Vitest。
  - OpenAI 网关、调度、token refresh 通过 unit/integration 编译与测试。

## 测试记录

- `cd backend && go test -tags=unit ./...`：通过。首次运行暴露签名/测试同步问题，修复后重跑通过。
- `cd backend && make test-unit`：通过。
- `cd backend && go test -tags=integration ./...`：通过。
- `cd backend && make test-integration`：通过。
- `cd backend && golangci-lint run ./...`：通过，`0 issues.`。
- `pnpm --dir frontend run lint:check`：通过。
- `pnpm --dir frontend run typecheck`：通过。
- `pnpm --dir frontend run test:run`：通过，136 个测试文件 / 874 个测试。
- `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.145`。
- `cat backend/cmd/server/VERSION`：`0.1.145`。
- `make build`：通过；Vite 输出既有动态/静态 import 和大 chunk 警告。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- `bash deploy/version_resolver.sh resolve .`：`0.1.145`。
- `bash scripts/collect_upstream_sync_context.sh --no-fetch`：完成，记录 `latest_upstream_tag=v0.1.145`、`local_version=0.1.145`、`upstream_version=0.1.145`，仍提示高风险重叠用于评审关注。
- 部署后版本输出 / 页面版本人工核对：尚未执行，留到 push deploy 线上验收阶段核对。

## 剩余风险与观察点

- 请求头覆写属于新可配置转发面，需在 review 里重点看禁止名单、批量清空防护和 OpenAI/Anthropic 对称性是否在本地定制路径下仍成立。
- OpenAI 高级调度器本轮同时变更后端权重、设置 API、前端配置和测试；虽然本地测试已覆盖，线上发布后仍需观察账号选择、粘性会话和模型能力过滤。
- 内建支付 CNY 汇率字段与 APIPool iframe 充值入口并存，需确认上线后不会误导用户把内建支付入口当作唯一充值方式。
- 部署回滚建议：若发布后应用异常，优先使用 `cd /opt/sub2api/deploy && ./rollback.sh image`；只有确认数据层异常时才考虑 `db-restore --with-image`。

## 结论

- 建议保留当前同步结果，进入双 review 阶段。当前代码已合入 `upstream/main` 到 `v0.1.145`，版本文件对齐；本地回归和构建通过，剩余风险应通过 requesting-code-review / gstack review 再审一轮。
