# 上游同步评审记录（2026-07-03）

## 基线

- 当前分支：`main`
- 同步 merge commit：`54a35c776d60b6d0db72269c818aa5b29fe2da8c`
- 评审修复提交：本提交（`fix(sync): address v0.1.143 review findings`）
- 合入前本地基线：`87af59d70e66f67c885fc2dd0aabffe99e022eda`
- 上游引用：`upstream/main`
- 上游 SHA：`0b8e5eec32c2a6721d5d89e4ab7db23e0bb85e50`
- merge-base：`7dc7cfce1db5d31599815ff29acf6847ead0f0b7`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.143`
- upstream/main 版本文件：`0.1.143`
- 本地最终 VERSION：`0.1.143`

## 上游更新摘要

- 合入 upstream/main `7dc7cfce1..0b8e5eec3`，对应 upstream release `v0.1.143`。
- 主要吸收内容：
  - OpenAI/Gateway：compact passthrough、count_tokens scope error、Codex image bridge 对 compact 路径的跳过逻辑。
  - Grok media：新增/修正 Grok image alias 与媒体生成默认 gate。
  - 分组/计费：高峰时段倍率、分组列设置、server timezone/UTC offset public settings。
  - 订阅/账号：OpenAI 订阅过期时间持久化、inactive workspace plan type 保留、已撤销订阅恢复。
  - 前端/运维：Usage IP geolocation、充值到期展示、README sponsor/docs 更新。
- 命中的高风险模块：
  - `backend/internal/service/openai_gateway_service.go`
  - `backend/internal/service/openai_oauth_passthrough_test.go`
  - `backend/internal/service/openai_oauth_service.go`
  - `backend/internal/handler/openai_gateway_handler.go`
  - `backend/internal/server/api_contract_test.go`
  - `deploy/.env.example`
  - `deploy/config.example.yaml`
  - `frontend/src/components/admin/usage/UsageTable.vue`
  - `README*.md`

## 本地定制保护点

- 品牌与文案：`README.md` 保留 APIPool 当前中文项目说明、DigitalOcean 部署说明和备份/回滚链路；未接受 upstream Sub2API sponsor 长文档覆盖。
- 部署/回滚/版本链路：`backend/cmd/server/VERSION` 已对齐 `0.1.143`；保留 GHCR 构建、服务器拉取精确 commit 镜像、部署前数据库备份和 `deploy-sub2api:rollback-latest` 快速回滚链路。
- OpenAI OAuth / Codex：保留本地 OAuth passthrough normalization 测试，同时吸收 upstream compact failover 语义；compact 全局 fallback 已接入 runtime。
- Grok media：保留 APIPool 的显式 opt-in 边界，不静默把既有 Grok 分组的 `allow_image_generation=false` 改成 true。
- Usage IP geolocation：第三方 IP 查询按钮与批量查询默认关闭，避免后台 usage log IP 在没有配置/审计开关时外传到第三方服务。

## 冲突与取舍

- Git 冲突文件：
  - `README.md`：保留 APIPool 本地品牌、部署、备份/回滚和 iframe 充值并存说明。
  - `backend/internal/service/openai_oauth_passthrough_test.go`：同时保留本地 normalization 测试与 upstream compact retry/failover 语义。
- 双 review 发现并已处理：
  - Compact 全局配置：`GATEWAY_OPENAI_COMPACT_MODEL` 已从配置进入 `/responses/compact` 普通转发与 OAuth passthrough 转发；账号级 compact mapping 仍优先。
  - Grok image generation：迁移 158 不再批量打开既有 Grok 分组；创建分组时 `allow_image_generation` 改为三态，未传字段走平台默认，显式 false 得以保留。
  - IP geolocation 外传：`UsageTable` 新增 `enableIpGeo` 显式开关并默认 false；父级未传入时只展示原始 IP，不渲染第三方查询控件。
- 合入后修复：
  - `backend/internal/server/api_contract_test.go`：public settings contract 跟进 upstream 新增 `server_timezone` / `server_utc_offset`。
  - `backend/migrations/auth_identity_payment_migrations_regression_test.go`：迁移 158 回归测试改为锁定 APIPool opt-in 语义。

## 测试记录

- `cd backend && go test -tags=unit ./internal/service -run 'OpenAIPassthrough|NormalizeOpenAIPassthrough|PeakMultiplier'`：通过。
- `cd backend && go test -tags=unit ./internal/server`：通过。
- `cd backend && go test -tags=unit ./internal/service -run 'Compact|GrokMedia|CreateGroup'`：通过。
- `cd backend && go test -tags=unit ./migrations`：通过。
- `cd backend && go test -tags=unit ./...`：通过。过程中先修复迁移 158 回归测试语义后重跑通过。
- `cd backend && make test-unit`：通过。
- `cd backend && go test -tags=integration ./...`：通过。
- `cd backend && make test-integration`：通过。
- `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- `pnpm --dir frontend run lint:check`：通过。
- `pnpm --dir frontend run typecheck`：通过。
- `pnpm --dir frontend run test:run`：通过，`134 passed / 829 tests`。输出含既有测试 stderr / Browserslist 提示。
- `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.143`。
- `cat backend/cmd/server/VERSION`：`0.1.143`。
- `make build`：通过；Vite 仅输出既有 dynamic import / chunk size 警告。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- `bash deploy/version_resolver.sh resolve .`：`0.1.143`。
- `bash scripts/collect_upstream_sync_context.sh --no-fetch`：通过；本地最终 VERSION 与 upstream/main VERSION 均为 `0.1.143`。
- 部署后线上 runtime version / 页面版本待 push deploy 后用服务器容器与前台再次核对。

## 剩余风险与观察点

- 本轮上游 only commit 53 个，主要风险集中在 OpenAI compact/OAuth passthrough、Grok media gate、分组高峰倍率和 usage IP geolocation。
- IP geolocation 目前在 APIPool 后台默认关闭；如后续要开放，需要补充后端配置/审计/隐私提示，再由父级显式传入 `enableIpGeo`。
- 发布后重点观察：
  - `/v1/responses/compact` 普通转发与 OAuth passthrough 是否按 `GATEWAY_OPENAI_COMPACT_MODEL` 转成 `gpt-5.4`。
  - Grok 既有分组是否保持当前图片生成开关，新增 Grok 分组未传字段时是否按平台默认开启。
  - 管理后台 usage 表是否不再默认触发第三方 IP 查询。
  - 运行时版本是否显示 `0.1.143`。
- 自动部署会在服务器上做 pre-deploy 数据库备份并刷新回退镜像；快速回滚仍优先使用：`cd /opt/sub2api/deploy && ./rollback.sh image`。

## 结论

- 建议保留当前 merge 与评审修复结果，并进入 push deploy 阶段。两路代码评审提出的阻断项均已处理，本地单元、集成、lint、前端测试、构建和 compose 配置检查均已通过。
