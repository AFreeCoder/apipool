# 上游同步评审记录（2026-07-04）

## 基线

- 当前分支：`main`
- 合入前本地基线：`e03be670002a432658b64d8f95c6c3a26b0d091f`
- 上游引用：`upstream/main`
- 上游 SHA：`b650bdd68d25bad3e502b2e34efe775555da2eba`
- merge-base：`0b8e5eec32c2a6721d5d89e4ab7db23e0bb85e50`
- 同步方式：`git merge upstream/main`
- merge commit：`6903eabbeeefae4ecb161561cbede797941e288b`
- 验证修复 commit：`2011b1b58`
- upstream 最新 release tag：`v0.1.144`
- upstream/main 版本文件：`0.1.144`
- 本地最终 VERSION：`0.1.144`

## 上游更新摘要

- 吸收 upstream `v0.1.144` 的 37 个提交，运行时版本从 `0.1.143` 对齐到 `0.1.144`。
- 主要变更包括：Codex session 导入避免 access-token-only 账号误合并、Codex 图片工具策略收敛、Responses 用 mapped billing model 记账、Anthropic Fable `7d_oi` 窗口按模型级限流、`token_expired` 刷新不可重试、Antigravity Gemini 路由归一、usage 错误请求 UI/排序/筛选、并发槽清理、group capacity SQL 与批量汇总优化、setup migration timeout 配置。
- 高风险命中：`backend/internal/service/openai_*`、`backend/internal/service/ratelimit_service.go`、`backend/internal/handler/admin/account_codex_import.go`、`deploy/*.yml`、`deploy/.env.example`、`README*`。

## 本地定制保护点

- 品牌与文案：`README.md` 保留 APIPool 精简首页、生产域名、DigitalOcean 部署说明和本地开发命令，只吸收 upstream 新增的无商业授权声明与 Grok media 能力说明；`README_CN.md` 移除 upstream 已删除的 Claude Code Plan Mode 过时 known issue；`README_JA.md` 接受 upstream 文案更新。
- 部署、回滚、版本链路：`.github/workflows/deploy.yml`、`deploy/rollback.sh`、`deploy/docker-compose.deploy.yml`、`deploy/version_resolver.sh` 仍存在；`deploy/.env.example` 与 compose 文件吸收 `SETUP_MIGRATION_TIMEOUT_SECONDS`。
- OpenAI OAuth / Codex：保留 APIPool 既有 Codex/OAuth、reqlog 和图片桥接逻辑，同时吸收 upstream 的 explicit image tool policy、mapped billing model、access-token-only import 保护。
- Kiro / OpenClaw：`backend/internal/server/routes/admin.go`、`backend/internal/handler/wire.go`、`backend/internal/service/wire.go`、`frontend/src/api/admin/kiro.ts`、`frontend/src/composables/useKiroOAuth.ts`、`frontend/src/utils/openclawConfig.ts` 仍保留 Kiro/OpenClaw 路由与前端导入能力。
- 购买入口：`/purchase`、`PurchaseSubscriptionView.vue`、`purchase_subscription_enabled` / `purchase_subscription_url` 设置链路仍保留，未被 upstream 内建支付入口替换。
- reqlog：`backend/internal/pkg/reqlog`、`backend/internal/server/middleware/reqlog_capture.go`、handler request body capture 点仍保留。

## 冲突与取舍

- Git 冲突文件：`README.md`、`README_CN.md`。
- `README.md` 处理：保留 APIPool 项目结构和部署说明；不引入 upstream 的完整 sponsor table / Sub2API README 结构；补入无商业授权声明、Grok media models 与分组图片权限说明。
- `README_CN.md` 处理：接受 upstream 删除 Claude Code Plan Mode known issue 的方向，清掉过时段落。
- 无 Git 冲突但已复核的热点：OpenAI gateway、Codex transform、rate limit、deploy compose、Kiro/OpenClaw、purchase iframe、reqlog。

## 验证修复

- `backend/internal/server/api_contract_test.go`：`server_utc_offset` 期望改为按当前 `time.Local` 动态生成，避免在非 UTC+8 开发机上失败。
- `frontend/src/__tests__/setup.ts`：补全全局 `requestAnimationFrame` / `cancelAnimationFrame` mock，避免 `KeyUsageView` 异步动画在 Vitest teardown 后触发 unhandled error。
- 集成测试曾因并发执行 `go test -tags=integration ./...` 与 `make test-integration` 争抢 testcontainers/Docker 出现 Redis 容器探针超时；清理本轮残留的 `org.testcontainers` Redis 容器后串行复跑通过。

## 测试记录

- `cd backend && go test -tags=unit ./...`：通过。
- `cd backend && make test-unit`：通过。
- `cd backend && go test -tags=integration ./...`：串行复跑通过。
- `cd backend && make test-integration`：通过。
- `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- `pnpm --dir frontend run lint:check`：通过。
- `pnpm --dir frontend run typecheck`：通过。
- `pnpm --dir frontend run test:run`：通过，`134` 个 test files / `836` 个 tests。
- `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.144`。
- `cat backend/cmd/server/VERSION`：`0.1.144`。
- `bash deploy/version_resolver.sh resolve .`：`0.1.144`。
- `make build`：通过；Vite 输出既有 chunk size / dynamic import warning。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- `bash scripts/collect_upstream_sync_context.sh --no-fetch`：通过；确认本地版本、upstream 版本与 `v0.1.144` 对齐。

## 剩余风险与观察点

- 本轮触达 OpenAI/Codex、rate limit、usage UI、部署样例和 README，高风险区域需要双 code review 继续重点看语义冲突。
- 发布前仍需按 `apipool-push-deploy` gate 复核 GitHub Actions 部署流、数据库备份、rollback image 与线上容器健康。
- `make build` 的 Vite chunk warning 是既有构建体积提示，本轮未调整拆包策略。

## 结论

- 建议保留当前 upstream merge 结果和验证修复，进入双 subagent code review。
- 当前准备推送的本地提交链：`6903eabbe` upstream merge、`2011b1b58` 验证修复、本评审记录提交。
