# 上游同步评审记录（2026-07-01）

## 基线

- 当前分支：`main`
- 当前 HEAD：`78464600051d343b10b5df783d55562a05da822e`
- 合入前本地基线：`78464600051d343b10b5df783d55562a05da822e`
- 上游引用：`upstream/main`
- 上游 SHA：`7dc7cfce1db5d31599815ff29acf6847ead0f0b7`
- merge-base：`dc1bc15457db60bceabb90fd5b8e2120b449f906`
- 同步方式：`git merge upstream/main`
- upstream 最新 release tag：`v0.1.142`
- upstream/main 版本文件：`0.1.142`
- 本地最终 VERSION：`0.1.142`

## 上游更新摘要

- 合入 upstream/main `dc1bc1545..7dc7cfce1`，对应 upstream release `v0.1.142`。
- 主要吸收内容：
  - Grok media routing：新增 Grok 图片生成/编辑、视频生成/查询相关 handler/service/model id 支持。
  - OpenAI Spark shadow account：新增 shadow parent、quota headroom、credential owner、调度与后台账号 UI 支持。
  - OpenAI Codex/OAuth：保留 encrypted reasoning、修正 GPT-5.5 Pro Codex 模型名、移除默认模型中的 gpt-5.3-codex。
  - Anthropic OAuth：新增 dateline 指纹规整，减少客户端隐写指纹透传。
  - 订阅/支付/用户用量：修复订阅撤销软删除、使用记录默认显示推理强度列。
  - 部署/版本：新增 `backend/scripts/resolve-version.sh`，Docker 构建版本解析支持精确 tag。
  - 前端/i18n：修复英文 fallback、Grok 图标/配色/文案和 interval 校验文案。
- 命中的高风险模块：
  - `backend/internal/service/openai_codex_transform.go`
  - `backend/internal/service/openai_gateway_service.go`
  - `backend/internal/server/routes/gateway.go`
  - `backend/internal/service/ratelimit_service.go`
  - `backend/cmd/server/wire_gen.go`
  - `deploy/Dockerfile`
  - `frontend/src/views/admin/SettingsView.vue`
  - `README*.md`

## 本地定制保护点

- 品牌与文案：`README.md` 保留 APIPool 当前中文项目说明、生产端点、DigitalOcean 部署说明和 iframe 充值并存说明；未引入 upstream README 中的 Sub2API sponsor/通用安装段。
- 部署/回滚/版本链路：保留 APIPool GHCR/DigitalOcean 构建部署链路；`deploy/Dockerfile` 采用 upstream tag-aware version resolver，同时保留本地 `-trimpath`。
- OpenAI OAuth / Codex 兼容：保留本地 unknown top-level item 测试；接受 upstream encrypted reasoning 合同，reasoning item 保留 `encrypted_content` 并剥离 `rs_*` id。
- Kiro / OpenClaw：`wire_gen.go` 继续传入本地 `kiroTokenProvider`，没有被 upstream Spark shadow 变更覆盖。
- 请求日志/审计：顶层 `/images/*` 与 `/videos/*` 路由使用 upstream Grok media handler，同时保留本地 `reqLogCapture` 中间件。
- OpenAI OAuth 401：合并本地 OpenAI OAuth 错误状态判断和 upstream Spark shadow credential owner 处理，401 写入母账号而不是影子账号。

## 冲突与取舍

- Git 冲突文件：
  - `.gitignore`：合并保留 `scripts/collect_upstream_sync_context.sh`、`scripts/scaffold_sync_review_doc.sh` 和新增 `backend/scripts/resolve-version.sh`。
  - `README.md`：保留 APIPool 本地品牌/部署说明，不吸收 upstream Sub2API sponsor/通用安装段。
  - `backend/cmd/server/wire_gen.go`：同时保留 `kiroTokenProvider` 和 upstream 新增 `openAIQuotaService`。
  - `backend/internal/server/routes/gateway.go`：吸收 Grok image/video handlers，并补回 `reqLogCapture`。
  - `backend/internal/service/openai_codex_transform_test.go`：同时保留本地 unknown item 测试和 upstream encrypted reasoning 测试。
  - `backend/internal/service/ratelimit_service.go`：401 路径合并 OAuth error-state 与 Spark shadow credential owner。
  - `backend/internal/service/ratelimit_service_401_test.go`：测试 stub 同时记录 `lastTempMsg` 和 `lastTempID`。
  - `deploy/Dockerfile`：采用 tag-aware resolver，保留 `-trimpath`。
- 合入后修复：
  - `backend/internal/service/openai_ws_forwarder_success_test.go`：跟进 `buildOpenAIWSHeaders` 新签名，传入 `context.Context` 并接收 3 个返回值。
  - `backend/internal/service/auth_service_email_bind_test.go`：平台配额快照断言跟随 `AllowedQuotaPlatforms`，新增 Grok 空限额快照断言。
  - `frontend/src/components/account/__tests__/BulkEditAccountModal.spec.ts`：跟进 Antigravity preset 文案从中文 `透传` 到英文 `passthrough`。

## 测试记录

- `cd backend && go test -tags=unit ./...`：通过。过程中先修复 `buildOpenAIWSHeaders` 测试签名和 platform quota 断言后重跑通过。
- `cd backend && go test -tags=integration ./...`：通过。
- `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- `pnpm --dir frontend run lint:check`：通过。
- `pnpm --dir frontend run typecheck`：通过。
- `pnpm --dir frontend run test:run`：通过，`130 passed / 785 tests`。过程中先修复 Antigravity preset 文案断言后重跑通过。
- `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.142`。
- `cat backend/cmd/server/VERSION`：`0.1.142`。
- `bash backend/scripts/resolve-version.sh`：`0.1.142`。
- `bash deploy/version_resolver.sh resolve .`：`0.1.142`。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- `make build`：通过；Vite 仅输出既有 chunk size / dynamic import 警告。
- `bash scripts/collect_upstream_sync_context.sh --no-fetch`：通过；本地最终 VERSION 与 upstream/main VERSION 均为 `0.1.142`。
- 未单独运行 `make test-unit` / `make test-integration`，因为已运行其等价命令 `go test -tags=unit ./...` 与 `go test -tags=integration ./...`。
- 部署后线上 runtime version / 页面版本待 push deploy 后用服务器容器与页面再次核对。

## 剩余风险与观察点

- 本轮一次性引入 181 个文件级变更，主要风险在 OpenAI/Grok media routing、Spark shadow account、platform quota、Codex encrypted reasoning 和部署版本解析。
- README_CN / README_JA 仍是 upstream 文案高风险区域，本轮接受 upstream 小幅 sponsor/doc 更新，但 APIPool 中文/日文完整品牌重写仍不是本次 scope。
- `assets/partners/logos/anpin.jpg` 来自 upstream sponsor 更新，当前保留；主 README 未引入 sponsor 表。
- 发布后重点观察：
  - OpenAI OAuth / Codex `/responses` 与 WS v2 的 reasoning replay。
  - Grok image/video 路由是否按 group platform 正确 gated。
  - Spark shadow account 401/temp-unschedulable 是否落到母账号。
  - 运行时版本是否显示 `0.1.142`。
- 快速回滚仍优先使用镜像回滚：`cd /opt/sub2api/deploy && ./rollback.sh image`。

## 结论

- 建议保留当前 merge 结果并进入双 review / push deploy 阶段。合入后本地主要回归、构建和部署配置检查均已通过，剩余风险应通过代码评审和线上部署健康检查继续收口。
