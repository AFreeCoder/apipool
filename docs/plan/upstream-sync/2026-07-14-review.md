# 上游同步评审记录（2026-07-14）

## 基线

- 当前分支：`main`
- 合入前基线：`HEAD`
- 合入前 SHA：`d3a4cd81df809f7b4a1e4f2092753a64e9295c77`
- 上游引用：`upstream/main`
- 上游 SHA：`da85cc7e47882090b115d664afe8e39b37aa7417`
- merge-base：`55ed0ab0da367183d97c15659e33ae9e83f6ff90`
- 同步方式：`git merge upstream/main`
- 合并提交：`5710f2e0e41ad7ae98458051aca40ef9cc2d4952`
- 当前运行时版本：`0.1.155`
- upstream/main 版本文件：`0.1.155`
- upstream 最新 release tag：`v0.1.155`

## 上游更新摘要

- 上游从 `v0.1.153` 更新到 `v0.1.155`，主要包含 Grok Web SSO/导入探测/渠道监控、可选 Server-Timing 指标、系统日志 host 过滤、OpenAI 长上下文计费开关与迁移、Codex models API Key 上游与账号故障转移、Responses namespace 兼容、图片生成 keepalive/最终状态、调度快照重建与代理/账号到期事件修复。
- 高风险模块命中：OpenAI/Codex 网关请求与响应改写、计费与用量日志、调度快照、代理到期切换、账号 OAuth/导入、Ent schema/迁移、Redis/HTTP client、前端账号表单、Wire 依赖装配。
- 额外吸收但不能覆盖本地逻辑的实现：Codex models 的 API Key/OAuth 故障转移和缓存、Responses namespace 还原、代理到期的定向调度通知、Server-Timing client 传播、Grok SSO/监控；同时保留 APIPool 的 Kiro、视频字段、请求日志、Codex 默认客户端版本和 `output: null` 兼容逻辑。
- 版本链路已对齐：`backend/cmd/server/VERSION`、`upstream/main` 版本文件、最新合入 tag 和 `deploy/version_resolver.sh` 均为 `0.1.155` / `v0.1.155`。

## 本地定制保护点

- 品牌/APIPool 文案与入口：冲突的 `README.md` 保留本地 APIPool 品牌和部署说明；`/purchase` iframe 路由仍存在。
- 部署、回滚、版本链路：`.github/workflows/deploy.yml`、`deploy/rollback.sh`、`deploy/docker-compose.deploy.yml`、`deploy/version_resolver.sh` 相对合入前无变化；版本文件更新为 `0.1.155`。
- OpenAI OAuth / Codex 兼容逻辑：保留本地默认 `codexCLIVersion`、OAuth 兼容、Responses 的 `output: null` 归一化、请求明细日志和视频用量字段，并叠加上游 Codex manifest 故障转移与 namespace 还原。
- 管理后台行为与默认配置：Kiro OAuth/账号表单逻辑与测试继续保留；Grok SSO、导入探测和渠道监控按上游新增；长上下文计费沿用上游最终默认关闭且可按账号控制的语义。

## 冲突与取舍

- 显式 Git 冲突共 13 个：`README.md`、`backend/cmd/server/wire_gen.go`、`backend/internal/handler/dto/mappers.go`、`backend/internal/handler/openai_codex_models_handler.go`、`backend/internal/pkg/antigravity/client.go`、`backend/internal/repository/proxy_repo.go`、`backend/internal/service/openai_codex_models_service.go`、`backend/internal/service/openai_codex_models_service_test.go`、`backend/internal/service/openai_gateway_passthrough.go`、`backend/internal/service/openai_gateway_response_handling.go`、`frontend/src/components/account/CreateAccountModal.vue`、`frontend/src/components/account/OAuthAuthorizationFlow.vue`、`frontend/src/components/account/__tests__/CreateAccountModal.spec.ts`。
- 无冲突但重点语义审查：部署/回滚工作流、Ent schema 与迁移、账号到期及代理到期事件、调度 outbox/snapshot、用量计费、请求日志、Grok 导入/监控、前端 Edit/Create Account 表单和 Ops 日志 UI。
- 取舍结果：Wire 使用 `v0.7.0` 重新生成；DTO 同时保留视频字段并吸收 `LongContextBillingApplied`；Codex models 吸收 API Key/OAuth 故障转移及缓存并保留本地默认客户端版本；代理到期同时保留本地原子状态条件与上游定向 `account_bulk_changed`；Responses 先还原 namespace 再执行本地空输出归一化；前端同时保留 Kiro 流程并新增 Grok SSO/手动导入参数。
- 版本文件随合并提交 `5710f2e0e` 一并更新，没有额外拆分版本提交。

## 测试记录

- [x] `cd backend && go test -tags=unit ./...`：通过。
- [x] `cd backend && make test-unit`：通过。
- [x] `cd backend && go test -tags=integration ./...`：通过。
- [x] `cd backend && make test-integration`：通过。
- [x] `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- [x] `pnpm --dir frontend run lint:check`：通过。
- [x] `pnpm --dir frontend run typecheck`：通过。
- [x] 前端 Vitest：158 个测试文件、1041 个测试全部通过；仅出现既有测试 stderr 警告。
- [x] `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.155`。
- [x] `cat backend/cmd/server/VERSION`：`0.1.155`。
- [x] `make build`：通过；后端以 `Version=0.1.155` 构建，前端仅有既有动态导入/大 chunk 警告。
- [x] `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- [x] `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- [x] `bash deploy/version_resolver.sh resolve .`：`0.1.155`。
- [x] `./.agents/skills/apipool-sync-upstream/scripts/collect_upstream_sync_context.sh --no-fetch`：合并后 `merge-base` 等于 `upstream/main`，`Upstream-only commits` 为 none。
- [x] 生成检查：`go generate ./ent` 与 Wire `v0.7.0` 生成均完成，生成文件已纳入合并。
- [x] 专项验证：repository/service/handler、`pkg/antigravity`、`pkg/apicompat` 目标包测试通过；Kiro provider、请求日志清理、`/purchase` 路由均仍存在。
- [x] 合并中发现并修复两个本地组合问题：`proxy_repo.go` 重复声明 `err`；Ops handler 测试仍使用旧构造签名。修复后重新执行全量验证通过。
- [ ] 部署后版本输出 / 页面版本人工核对：不属于同步阶段，将由 `apipool-push-deploy` 阶段完成。

## 剩余风险与观察点

- 本次变更横跨网关、计费、调度、迁移和前端账号管理，虽然本地全量验证通过，仍需完成两路独立代码评审后才能推送生产。
- 生产部署后重点观察：数据库迁移 174/175/175a/176、Codex models API Key/OAuth 回退、长上下文计费标记、Grok OAuth 探测、代理/账号到期后的调度重建、图片生成非流式 keepalive。
- 回滚预案沿用现有双数据库备份与前镜像元数据链路；推送前需再次核对 workflow、备份和 rollback 脚本，部署后验证主/业务库备份可解压及上一镜像可定位。

## 结论

- 同步阶段建议保留当前 merge 结果并进入双路独立评审；在 receiving review 完成前不作生产推送结论。
- 合并提交：`5710f2e0e41ad7ae98458051aca40ef9cc2d4952`；目标版本/tag：`0.1.155` / `v0.1.155`；当前尚未推送 `origin/main`。
