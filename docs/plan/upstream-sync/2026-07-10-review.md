# 上游同步评审记录（2026-07-10）

## 基线

- 当前分支：`main`
- 合入前本地基线：`c28a438b6eee31298c03f64aedd4b93993e7278c`
- 上游引用：`upstream/main`
- 上游 SHA：`e316ebf52838a89d57fc790981cce7520f819ac8`
- merge-base：`12d811bd76572836d6df6e1fa8aa5ff91be3b12e`
- 同步方式：`git merge upstream/main --no-edit`
- upstream 最新 release tag：`v0.1.151`
- upstream/main 版本文件：`0.1.151`
- 本地最终 VERSION：`0.1.151`
- 版本对齐：tag、upstream/main 的 VERSION 与本地最终 VERSION 一致，未拆分额外版本提交

## 上游更新摘要

- 合入 upstream `v0.1.150` 至 `v0.1.151`，共 60 个上游独有提交（其中 39 个非 merge 提交），最终上游 SHA 为 `e316ebf52838a89d57fc790981cce7520f819ac8`。
- API 兼容：Responses→Chat bridge 新增 `custom` / `tool_search` 支持、namespace 工具名冲突校验、tool choice 降级与回程 namespace 恢复。
- OpenAI/Codex：Codex 客户端身份头配对与最低版本收口、GPT-5.6 / max / cache-write 计费、用户级 Fast/Flex 策略、图片 `image_gen` namespace 声明清理、Windows WebSocket reset 识别。
- compact/流式：SSE→JSON 保留 raw `output_item.done`、unary 等待心跳、心跳并发写安全、终态缺 compaction 时从事件流补入。
- 计费/支付：支付履约并发与恢复加固、GPT-5.6 usage 完整性、usage effort 模型候选修复、`request_type` 解析与前端类型对齐。
- 其他：setup-token 纳入后台自动刷新、ops capture writer nil 守卫、Responses↔Anthropic `cache_creation_input_tokens` 补齐、迁移 `173_allow_cyber_blocked_usage_request_type.sql`。

## 本地定制保护点

- 品牌与入口：本轮 upstream 变更未触及 APIPool 的 README、Logo、首页文档、`/purchase` iframe 充值入口、AppHeader 跑马灯或 Kiro/OpenClaw 文件，未出现本地功能删除。
- 部署与回滚：`.github/workflows/deploy.yml`、`deploy/rollback.sh`、`deploy/version_resolver.sh`、`deploy/docker-compose.deploy.yml` 未被本轮 upstream 覆盖；DigitalOcean 数据库备份、rollback image 与健康检查语义保持不变。
- OpenAI OAuth / Codex：保留本地 UA 规范化、结构化错误码、图片工具策略与图片 token 用量字段，同时吸收 upstream 的 Codex 官方身份配对、compact 心跳收口与 tool namespace 修复。
- 计费：`BillingService` 同时保留本地 `ImageInputPricePerToken` / `ImageOutputPricePerToken`，并吸收 upstream 新增的 `CacheCreationPricePerTokenPriority`；没有把图片 token 计费退回 `$0` 或丢失 priority cache-write 价格。
- 前端默认值：保留 GPT-5.5 Codex 默认配置、跑马灯设置 fallback 与 APIPool provider 文案，同时吸收 GPT-5.6 OpenCode 模型/variant 配置和公开设置并发请求复用。

## 冲突与取舍

- Git 冲突文件：
  - `backend/internal/handler/openai_gateway_handler.go`
  - `backend/internal/service/account_usage_service.go`
  - `backend/internal/service/billing_service.go`
  - `backend/internal/service/openai_gateway_passthrough.go`
  - `backend/internal/service/openai_gateway_response_handling.go`
  - `backend/internal/service/openai_gateway_service_test.go`
  - `backend/internal/service/openai_ws_forwarder_payload.go`
  - `frontend/src/components/keys/__tests__/UseKeyModal.spec.ts`
- error handler 同时保留本地 `errorResponseWithCode` / SSE 错误 `code`，并在 compact 心跳已提交 200 时先停心跳、转为协议合规的 `response.failed`。
- passthrough / WS 采用 upstream 的 `enforceCodexIdentityHeaders` 作为 OAuth 最终身份收口，保留本地斜杠 UA 规范化；删除被新逻辑完整替代的旧“非 Codex UA 一律兜底”分支，避免误改官方 `codex-tui` 身份。
- usage 解析同时保留图片输入/输出 token，并接入 upstream cache read / cache creation helper；测试同时覆盖官方嵌套字段优先级、兼容别名和图片 token。
- 前端冲突测试同时保留 APIPool 的 GPT-5.5 Codex 默认配置断言与 upstream 的 GPT-5.6 OpenCode variant 断言。
- 首轮前端测试发现 upstream 新增的 `createPublicSettings()` 测试工厂漏掉 APIPool 跑马灯字段，导致刷新后缓存返回值多出规范化字段；只补齐测试工厂的 `marquee_enabled=false` / `marquee_messages=[]`，生产逻辑未改。
- 无冲突但已复核的热点包括 `image_generation_intent.go`、`openai_gateway_forward.go`、`openai_gateway_service.go`、`frontend/src/router/index.ts`、`frontend/src/stores/app.ts` 与 `SettingsView.vue`。
- 同步上下文脚本的高风险匹配器补入 `account_usage_service`、`billing_service`、`openai_ws` 与 `UseKeyModal`，确保后续同步不会漏报本轮真实冲突热点。

## 测试记录

- `cd backend && go test -tags=unit ./...`：通过。
- `cd backend && make test-unit`：通过。
- `cd backend && go test -tags=integration ./...`：通过。
- `cd backend && make test-integration`：通过。
- `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- `pnpm --dir frontend run lint:check`：通过。
- `pnpm --dir frontend run typecheck`：通过。
- `pnpm --dir frontend run test:run`：通过，`147` 个测试文件、`942` 个用例全部通过；输出包含既有错误分支 stderr 与 Browserslist 过期提示。
- 冲突热点专项：`go test -tags=unit ./internal/handler ./internal/service ./internal/pkg/apicompat ./internal/server/middleware` 通过。
- 前端失败用例修复后专项：`pnpm exec vitest run src/stores/__tests__/app.spec.ts -t 'force 在无活动请求时绕过缓存'` 通过。
- `go version`：`go1.26.4 darwin/arm64`，符合本仓库 CI 约束。
- `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.151`。
- `cat backend/cmd/server/VERSION`：`0.1.151`。
- `make build`：通过；仅有既有动态导入与 chunk-size warning。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- `POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- `bash deploy/version_resolver.sh resolve .`：`0.1.151`。
- `bash scripts/collect_upstream_sync_context.sh --no-fetch`：结构与高风险匹配校验通过；commit 后将再次复核最终 refs。
- 部署后版本、备份、rollback image、容器健康与外部 `/health`：待 `apipool-push-deploy` 阶段验证。

## 剩余风险与观察点

- 本轮直接命中 OpenAI/Codex 请求转换、图片工具策略、compact 流式收口和 GPT-5.6 计费，部署后需观察 `ops_error_logs`、`response.failed`、tool_search/MCP 工具调用和图片 token usage。
- 支付履约、订阅与兑换逻辑有并发恢复改动；本地 unit/integration 已通过，部署后仍需观察支付 webhook/fulfillment 错误率与数据库迁移结果。
- Fast/Flex 新增用户级规则，需关注后台配置保存、用户 ID scope 与未命中规则的 fallback 行为。
- 本轮没有删除 APIPool 路由、充值入口、Kiro/OpenClaw 或部署回滚能力；若上线异常，优先使用现有 image rollback，不做数据库恢复，除非确认持久化数据受损。

## 结论

- 建议保留本次 merge 结果并进入双 reviewer 阶段。
- 同步结果已通过后端 unit/integration、lint、前端 lint/typecheck/test、完整构建、Compose 渲染与版本解析；评审意见将在独立 receiving-review 阶段核验后再决定是否需要追加修复提交。
