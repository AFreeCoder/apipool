# 上游同步评审记录（2026-07-13）

## 基线

- 当前分支：`main`
- 当前 HEAD：`88e836034838e6d11f9623b960605c68e906eede`
- 合入前本地基线：`88e836034838e6d11f9623b960605c68e906eede`
- 上游引用：`upstream/main`
- 上游 SHA：`55ed0ab0da367183d97c15659e33ae9e83f6ff90`
- merge-base：`e316ebf52838a89d57fc790981cce7520f819ac8`
- 同步方式：`git merge upstream/main`
- 合入提交：`809e5e9fc8b34981aa1a3b66dc15c8035ceb871d`
- upstream 最新 release tag：`v0.1.153`
- upstream/main 版本文件：`0.1.153`
- 本地最终 VERSION：`0.1.153`

## 上游更新摘要

- 从本地 `0.1.151` 同步到 upstream `v0.1.153`，吸收 81 个 upstream-only commit。
- 主要能力：Grok OAuth 与 xAI API Key 双账号类型、Grok Chat/Responses/OpenCode 桥接、视频编辑与扩展、Codex `alpha/search` 路由及按次计费、OpenAI WS ingress 生命周期限制、Codex additional tools/Read 流式参数、调度缓存修复、API Key 最近 IP 查询索引、嵌入静态资源缓存、账号 `plan_type` 手动覆盖、Apple container 本地运维流程。
- 主要修复：Grok 账号诊断/额度/CLI 身份与上游模型同步、compact keepalive、Codex message item id、Anthropic 流式 stop reason、池模式同账号重试、前端 DataTable 小数据量滚动抖动与中文 i18n 缺口。
- 高风险模块：OpenAI/Codex/Grok 网关与 WebSocket、计费、调度缓存、账号编辑与密钥配置、部署样例、README、运行时版本链路。

## 本地定制保护点

- 品牌与文案：保留 APIPool 根 README、线上域名、DigitalOcean 运维口径和 iframe 充值说明；仅吸收 Grok/API Key 与 Apple container 的必要说明。`README_CN.md` / `README_JA.md` 继续作为已知 upstream 品牌债务观察。
- 部署/回滚/版本链路：`.github/workflows/deploy.yml`、`deploy/rollback.sh`、`deploy/docker-compose.deploy.yml`、`deploy/version_resolver.sh` 未被上游覆盖；`VERSION`、upstream tag、版本解析均为 `0.1.153`。
- OpenAI OAuth / Codex：保留 APIPool 的稳定错误码、实际 attempted model 展示、GPT Image 2 token 计费、请求明细采集和 compact/WS 本地修复，同时接入 Grok 动态平台识别、Web Search 按次计费和 alpha/search。
- Kiro / OpenClaw：保留 Kiro provider、管理路由、账号凭据构造、OAuth composable 与 OpenClaw 配置生成；账号编辑同时接入 upstream `plan_type`。
- 后台入口与默认配置：`/purchase` iframe 充值入口仍与内建支付并存；新 alpha/search、video edits/extensions 路由均补入 `reqLogCapture`；新增 WS ingress 默认值已进入后端配置与 `deploy/config.example.yaml`。

## 冲突与取舍

- 共处理 10 个 Git 冲突：`README.md`、`backend/internal/config/config_test.go`、`backend/internal/handler/no_account_error.go`、`backend/internal/handler/no_account_error_test.go`、`backend/internal/handler/openai_chat_completions.go`、`backend/internal/server/routes/gateway.go`、`backend/internal/service/openai_gateway_usage.go`、`frontend/src/components/account/EditAccountModal.vue`、`frontend/src/components/account/__tests__/credentialsBuilder.spec.ts`、`frontend/src/components/keys/__tests__/UseKeyModal.spec.ts`。
- 后端取舍：动态识别 OpenAI-compatible 分组平台并保留 APIPool attempted-model 错误语义；新增路由保留 reqlog；Web Search 按次计费先行分流，GPT Image 2 token 计费保持不变。
- 前端取舍：Kiro/OpenClaw 与 upstream Grok、OpenAI `plan_type` 同时保留；UseKeyModal 保留 APIPool Codex 配置测试并加入 Grok Build/OpenCode 覆盖。
- 无显式冲突但已复核：`openai_gateway_*`、`billing_service*`、`openai_ws_*`、`SettingsView.vue`、`deploy/.env.example`、`deploy/config.example.yaml`、Kiro wire/routes/composable、purchase iframe 路由和部署/回滚文件。

## 测试记录

- 通过：`cd backend && go generate ./ent`；生成结果与已合入 Ent 代码一致。
- 通过：`cd backend && go test -tags=unit ./...`。
- 通过：`cd backend && make test-unit`。
- 通过：`cd backend && go test -tags=integration ./...`。
- 通过：`cd backend && make test-integration`。
- 通过：`cd backend && golangci-lint run ./...`，输出 `0 issues.`。
- 通过：`pnpm --dir frontend run lint:check`。
- 通过：`pnpm --dir frontend run typecheck`。
- 通过：`pnpm --dir frontend run test:run`，152 个测试文件、983 个用例通过。
- 通过：`make build`；后端嵌入版本 `0.1.153`，前端生产构建完成；仅有既有动态/静态 import 与 chunk size 提示。
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.deploy.yml config -q`。
- 通过：`POSTGRES_PASSWORD=dummy docker compose -f deploy/docker-compose.local.yml config -q`。
- 通过：`bash deploy/version_resolver.sh resolve .`，输出 `0.1.153`。
- 通过：`bash deploy/tests/apple-container-test.sh`，完整 init/up/stop/down/delete lifecycle 通过。
- 通过：`bash scripts/check_deploy_workflow_biz_backup_order.sh`。
- 通过：`bash scripts/collect_upstream_sync_context.sh --no-fetch`；tag、upstream VERSION、本地 VERSION 均为 `0.1.153`。
- 部署后运行时版本、外部 `/health`、数据库备份与 rollback image：留给后续 `apipool-push-deploy` 阶段验证。

## 剩余风险与观察点

- 本轮触及网关、WebSocket、计费、调度与两条数据库迁移，虽本地全量回归通过，仍需双 review 独立检查语义冲突、并发与迁移安全。
- Grok OAuth/API Key、alpha/search 按次计费与 WS ingress 限制缺少本地真实上游凭据端到端请求；部署后重点观察 4xx/5xx、计费记录、连接数与调度日志。
- `README_CN.md` / `README_JA.md` 仍以 upstream Sub2API 文案为主，是既有品牌债务，不在本次同步中扩写。
- 最快安全回滚路径保持为 `cd /opt/sub2api/deploy && ./rollback.sh image`；仅数据异常时才考虑 `db-restore --with-image`。

## 结论

- 同步阶段建议保留 merge commit `809e5e9fc8b34981aa1a3b66dc15c8035ceb871d` 并进入双 review；评审发现中的 Critical/Important 项必须在部署前按 `receiving-code-review` 核验并处理。
