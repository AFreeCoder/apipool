# 上游同步评审记录（2026-07-16）

## 基线

- 当前分支：`main`
- 合入前基线：`HEAD^1`
- 合入前 SHA：`1c032621fa222f7acdbc181a5bb3c14ec634d3bc`
- 上游引用：`upstream/main`
- 上游 SHA：`bc2244c83fd8e92769d89ca01eb980513a720486`
- merge-base：`d515c3045ce838976ebedab87846aaaf893dbbf6`
- 同步方式：`git merge upstream/main`
- 合并提交：`0250f7aa00ff1efd9fb92f28e6e8de353dcd6bea`
- 合入前运行时版本：`0.1.156`
- 合并后运行时版本：`0.1.158`
- upstream/main 版本文件：`0.1.158`
- upstream 最新 release tag：`v0.1.158`

## 上游更新摘要

- 上游从 `v0.1.156` 更新到 `v0.1.158`，共引入 102 个非 merge 提交，合并结果相对 APIPool 基线涉及 388 个文件、约 2.8 万行新增。主要新增审计日志、会话 IP/User-Agent 绑定与敏感操作 2FA step-up、异步图片任务和 S3 存储、图片输入 token/价格、上游计费倍率探测、调度器上游成本权重、批量用户限制、分组/渠道监控复制，并包含 OpenAI Responses/WebSocket、Grok 自定义端点与 header override 等修复。
- 高风险模块命中：认证与管理员路由、审计/请求明细日志、OpenAI/Codex/Grok 网关、WebSocket 调度与粘连、计费及 usage log、账号凭据原子更新、代理关系、Ent schema/迁移、Wire 装配、前端账号表单与部署配置。
- 额外吸收但不能覆盖本地逻辑的实现：吸收上游审计、step-up、异步图片、S3、计费倍率探测和 `upstream_cost` 调度能力；同时保留 APIPool 的请求明细日志、Kiro/OpenClaw、Codex 兼容、`previous_response`/`session_sticky` 调度、图片输入费用独立记账、purchase iframe、备份恢复禁用和生产部署/回滚链路。
- 版本链路一致：`backend/cmd/server/VERSION`、`upstream/main` 版本文件、最新合入 tag 和 `deploy/version_resolver.sh` 均解析为 `0.1.158` / `v0.1.158`。

## 本地定制保护点

- 品牌/APIPool 文案与入口：冲突的 `README.md` 保留 APIPool 品牌、部署说明和 OpenClaw 用法，并补充上游异步图片任务文档入口；`/purchase` 仍使用现有 iframe 充值页。
- 部署、回滚、版本链路：生产仍为 DigitalOcean 单实例 Compose；部署前数据库备份、上一镜像元数据和 rollback 标签逻辑未被上游覆盖；版本更新为 `0.1.158`。
- OpenAI OAuth / Codex 兼容逻辑：保留本地 Codex 请求兼容、Responses/WebSocket `previous_response` 约束与粘连、终态/错误事件归一化、请求明细日志，并叠加上游请求体限制故障转移、Grok 图片意图识别和上游成本调度。
- 管理后台行为与默认配置：保留 Kiro/OpenClaw、后台备份恢复路由禁用、账号 header 模板能力和图片输入费用拆分；吸收上游审计日志、step-up、批量用户限制、Grok 自定义端点及复制操作。

## 冲突与取舍

- 显式 Git 冲突共 26 个，集中在 Wire/HTTP 路由、管理员路由、OpenAI 网关与响应处理、账号 repository、计费/定价、调度配置、Grok quota、账号创建/批量编辑/header override、UseKeyModal、多语言和 `README.md`。`git show --remerge-diff` 还覆盖了相关测试与语义修复文件。
- 无冲突但重点语义审查：审计日志和 ReqLog 中间件共存、所有图片/视频/异步任务路由的请求捕获、数据库迁移 177-181、代理关联关系、Agent Identity 导入、图片输入成本持久化、上游计费倍率 CAS、调度器多权重组合、purchase/backup 路由和部署工作流。
- 最终取舍：HTTP 清理流程同时关闭 `ReqLogSink` 与 `AuditLog`；管理员路由同时保留 ReqLog、审计与 step-up，下载备份需要 step-up 但恢复路由仍不注册；图片/视频/异步任务路由继续挂载 ReqLog；图片意图识别使用上游平台感知规则并保留本地 Codex defer gate；OpenAI 结果同时保留 upstream error event 与 terminal event；账号凭据继续使用本地 `MergeCredentials` 原子合并并接入上游 billing probe CAS；图片输入费用继续拆到 `ImageInputCost`；调度器同时保留 `upstream_cost`、`previous_response`、`session_sticky`；Agent Identity 导入保留显式过期时间并剥离 OAuth 凭据；header override 模板能力迁入上游新编辑器。
- 合并过程中额外修复了若干“无冲突但不能编译或语义退化”的组合问题：重复 pricing 字段、构造函数参数缺失、代理关系测试仍假设上游 O2O、header 模板入口丢失、Agent Identity 重复提前返回、图片输入成本测试仍按合并前口径断言。修复后重新生成 Ent/Wire 并执行全量验证。
- 版本文件由 upstream 合并直接更新为 `0.1.158`，无需额外 `chore(version)` 提交。

## 测试记录

- [x] `cd backend && GOMAXPROCS=2 GOTOOLCHAIN=auto GOFLAGS='-p=1' go test -tags=unit ./...`：通过。
- [x] `cd backend && make test-unit`：通过。
- [x] `cd backend && go test -tags=integration ./...`：通过。
- [x] `cd backend && make test-integration`：通过。
- [x] `cd backend && golangci-lint run ./...`：通过，`0 issues`。
- [x] `pnpm --dir frontend run lint:check`：通过。
- [x] `vue-tsc --noEmit`：通过；本机 Node 22 的原生模块签名异常会触发进程退出，改用本机 Node 20 并对 Rollup 原生依赖做本地临时签名后完成验证，未修改仓库源码。
- [x] 前端 Vitest：172 个测试文件、1199 个测试全部通过。
- [x] 冲突专项 Vitest：4 个测试文件、108 个测试全部通过。
- [x] `git tag --merged upstream/main --sort=-version:refname | head -1`：`v0.1.158`。
- [x] `cat backend/cmd/server/VERSION`：`0.1.158`。
- [x] `make build`：通过；仅有动态/静态导入和大于 500 KB chunk 的构建警告。
- [x] `POSTGRES_PASSWORD=validation-only REDIS_PASSWORD=validation-only docker compose -f deploy/docker-compose.deploy.yml config -q`：通过。
- [x] `POSTGRES_PASSWORD=validation-only REDIS_PASSWORD=validation-only docker compose -f deploy/docker-compose.local.yml config -q`：通过。
- [x] `bash deploy/version_resolver.sh resolve .`：`0.1.158`。
- [x] `./.agents/skills/apipool-sync-upstream/scripts/collect_upstream_sync_context.sh --no-fetch`：合并后 `merge-base` 等于 `upstream/main`，无剩余 upstream-only commit。
- [x] 生成检查：`go generate ./ent` 与 Wire 重新生成完成，生成文件已纳入合并。
- [x] 专项验证：审计/ReqLog 清理、OpenAI 终态、图片输入计费、billing probe CAS、代理关系、调度多权重、Agent Identity 导入、header override 模板、UseKeyModal 和 purchase/backup 路由均有对应测试或代码核对。
- [ ] 部署后版本输出 / 页面版本人工核对：不属于同步阶段，将由 `apipool-push-deploy` 阶段完成。

## 剩余风险与观察点

- 本次更新规模较大，新增迁移 177-181，并横跨认证、审计、计费、异步任务、S3 和调度热路径；本地全量验证通过，但必须完成两路独立代码评审和 receiving review 后才允许推送。
- 单实例部署会有短暂重启窗口。生产部署后重点观察：迁移执行、会话绑定/step-up、审计写入、异步图片任务、S3 配置缺省行为、图片输入费用、billing probe 调度、OpenAI/Grok 请求成功率和 ReqLog 队列清理。
- 回滚继续依赖部署前主库/业务库备份、上一镜像标签及镜像元数据；推送前和部署后都要验证备份文件、rollback-latest 和容器健康状态。

## 结论

- 同步阶段建议保留当前 merge 结果并进入双路独立评审；receiving review 完成前不作生产推送结论。
- 合并提交：`0250f7aa00ff1efd9fb92f28e6e8de353dcd6bea`；目标版本/tag：`0.1.158` / `v0.1.158`；当前尚未推送 `origin/main`。
